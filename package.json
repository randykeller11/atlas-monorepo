{
  "name": "nucoord-atlas",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server/index.mjs",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}
